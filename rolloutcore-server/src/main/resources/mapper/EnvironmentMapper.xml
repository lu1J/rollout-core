<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.lu1j.rolloutcore.server.mapper.EnvironmentMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ff_environment (project_id, env_key, name, status, created_at)
        VALUES (#{projectId}, #{envKey}, #{name}, #{status}, #{createdAt})
    </insert>
    <select id="findByKey" resultType="io.github.lu1j.rolloutcore.domain.Environment">
        SELECT id, project_id, env_key, name, status, created_at FROM ff_environment
        WHERE project_id = #{projectId} AND env_key = #{envKey}
    </select>
    <select id="listByProject" resultType="io.github.lu1j.rolloutcore.domain.Environment">
        SELECT id, project_id, env_key, name, status, created_at FROM ff_environment
        WHERE project_id = #{projectId} ORDER BY id
    </select>
</mapper>
