<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.lu1j.rolloutcore.server.mapper.FlagVariantMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ff_flag_variant (flag_id, variant_key, value_json, created_at)
        VALUES (#{flagId}, #{variantKey}, #{valueJson}, #{createdAt})
    </insert>
    <select id="findByKey" resultType="io.github.lu1j.rolloutcore.domain.FlagVariant">
        SELECT id, flag_id, variant_key, value_json, created_at FROM ff_flag_variant
        WHERE flag_id = #{flagId} AND variant_key = #{variantKey}
    </select>
    <select id="listByFlag" resultType="io.github.lu1j.rolloutcore.domain.FlagVariant">
        SELECT id, flag_id, variant_key, value_json, created_at FROM ff_flag_variant
        WHERE flag_id = #{flagId} ORDER BY id
    </select>
</mapper>
