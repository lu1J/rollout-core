<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.lu1j.rolloutcore.server.mapper.FeatureFlagMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ff_feature_flag (project_id, flag_key, name, value_type, status, created_at)
        VALUES (#{projectId}, #{flagKey}, #{name}, #{valueType}, #{status}, #{createdAt})
    </insert>
    <select id="findByKey" resultType="io.github.lu1j.rolloutcore.domain.FeatureFlag">
        SELECT id, project_id, flag_key, name, value_type, status, created_at FROM ff_feature_flag
        WHERE project_id = #{projectId} AND flag_key = #{flagKey}
    </select>
</mapper>
